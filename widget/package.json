{
  "name": "agent-keyboard-widget",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Embeddable Agent Keyboard prompt bar for editing the app it lives in.",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --dev",
    "check": "tsc --noEmit",
    "check:guest-demo": "node dev/check-guest-demo.mjs",
    "mock": "node dev/mock-server.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.24.2",
    "typescript": "^5.7.2"
  }
}
