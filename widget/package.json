{
  "name": "agent-keyboard-widget",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Embeddable Agent Keyboard prompt bar for editing the app it lives in.",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --dev",
    "check": "tsc --noEmit",
    "check:guest-demo": "node dev/check-guest-demo.mjs",
    "check:answered-options": "node dev/answered-options.check.mjs",
    "test": "npm run check && npm run check:guest-demo && npm run check:answered-options",
    "mock": "node dev/mock-server.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "typescript": "^5.7.2"
  }
}
